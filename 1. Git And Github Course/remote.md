1. add connections remotely (like github)
git remote add <<remote_name>> <<github_link>>

2. push branch into remote connections
git push <<remote_name>> <<branch_name>>
- if no branch name in remote (it will be created)
- if there is code will be updated